using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraLockStart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Spawn camera prefab
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
